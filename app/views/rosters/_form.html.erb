<%= form_with(model: roster) do |f| %>
  
  <p>
    <%= f.label :roster_date%>
    <%= f.date_field :roster_date %>
  </p>

  <p>
    <%= f.label :company_id%>
    <%= f.collection_select :company_id, Company.all, :id, :name %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>
